[package]
name = "dissonanza"
version.workspace = true
edition.workspace = true

[dependencies]
dissonanza-core = { path = "../core" }
slint = "1"
tokio = { version = "1", features = ["rt", "sync", "time", "macros"] }

[build-dependencies]
slint-build = "1"
