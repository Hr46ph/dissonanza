[package]
name = "dissonanza-core"
version.workspace = true
edition.workspace = true

[dependencies]
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync"] }
socket2 = "0.5"
if-addrs = "0.13"
